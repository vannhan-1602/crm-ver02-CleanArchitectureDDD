spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CRMOnline?useSSL=false&serverTimezone=UTC
    username: root 
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # Thêm dòng này
    show-sql: true
server:
  port: 8081